---
interface Props {
    href: string
    color: string
}

const {href, color} = Astro.props
---

<a
    class="bg-white rounded-xl size-12 flex items-center justify-center shadow"
    style={{color}}
    target="_blank"
    rel="noreferrer"
    href={href}><slot /></a
>
