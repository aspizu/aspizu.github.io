---
import ExternalLink from "@lucide/astro/icons/external-link"

interface Props {
    name: string
    description: string
    href: string
    repo?: string
    docs?: string
    image?: string
}

const {name, description, href, repo, docs, image} = Astro.props
---

<div
    class="Project z-1 transition-all p-0.5 rounded-md shadow-sm bg-cover bg-center bg-white/10"
    data-project={name}
    style={{
        backgroundImage: image ? `url('${image}')` : undefined,
    }}
>
    <div
        class="flex flex-col bg-linear-to-b from-black to-black/50 gap-1 p-4 rounded-sm shadow-sm"
    >
        <a rel="noreferrer" target="_blank" href={href} class="block">
            <h2 class="text-lg font-medium underline underline-offset-2">{name}</h2>
        </a>
        <p class="text-pretty text-neutral-200">{description}</p>
        <slot />
        <div class="flex items-start gap-2">
            {
                repo && (
                    <a
                        class="bg-white flex text-black shadow-sm text-sm font-semibold rounded-md pl-1.5 items-center gap-1 pr-1"
                        rel="noreferrer"
                        target="_blank"
                        href={repo}
                    >
                        GitHub
                        <ExternalLink class="size-4" />
                    </a>
                )
            }
            {
                docs && (
                    <a
                        class="bg-white flex text-black shadow-sm text-sm font-semibold rounded-md pl-1.5 items-center gap-1 pr-1"
                        rel="noreferrer"
                        target="_blank"
                        href={docs}
                    >
                        Documentation
                        <ExternalLink class="size-4" />
                    </a>
                )
            }
        </div>
    </div>
</div>
